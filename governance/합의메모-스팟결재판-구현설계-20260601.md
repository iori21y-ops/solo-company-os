# 합의메모 — 스팟 결재판 구현 설계 (2026-06-01)

## 확정 사항 (이전 대화 + 코드 실측)
- 트리거 방식: (a) 이벤트+폴링. 단 정기 발동 주체 부재 확인 → 이번 세션은 **스팟 경로만 cycle 1바퀴 구현**. 정기(schedule) 분기는 stub 자리만 둠.
- 정기/스팟 구분 기준(파트C §8 line41): schedule 트리거=정기, manual 트리거=스팟. 봇은 발동경로를 모름 → **trigger_type을 frontmatter에 명시**해야 함.
- 봇 직접 저장 확정(1번): 봇이 frontmatter(trigger_type 포함) 담은 마크다운을 pending에 직접 출력. 사장님 수동 저장은 백업.

## agent.js 구조 지도 (다음 세션 grep 반복 방지용)
- line 112~160: 결재판 커맨드(대기/승인/반려) — 사장님 수동, renameSync pending↔approved/rejected
- line 188~194: pending 저장부 writeFileSync(savePath, target.content) — 봇 메시지 텍스트 그대로, frontmatter 자동삽입 없음
- line 230: rawAnswer = await askClaude({...})
- line 237: parseCallProtocol(rawAnswer, registry) → {body:answer, calls, escalations}
- line 239: 봇 응답에 [승인]/[반려] → state.completed=true (채널 루프 종료 마킹만, 파일이동 무관)
- line 246: replyInChunks
- line 249: [CALL:] 처리
- line 302: async function askClaude({prompt, username, channelName, message}) — message 있으면 채널히스토리 포함
- line 521: parseCallProtocol — [ESCALATE] 파싱 → 채널 멘션(message.channel.send + 사장님 멘션)
- 관리팀장 모델: currentModel='opus' 이미 설정. 파일기반 검토는 runClaudeCli 직접 호출
- AGENTS_ROOT = ~/projects/_meta/agents, pending = AGENTS_ROOT/{에이전트}/outputs/pending/

## 구현 (str_replace 대상 4곳)
### ① content/SKILL.md 저장규칙 — 봇이 frontmatter 포함 출력하게
- 대상: line 124~133 산출물 저장규칙 블록
- 변경: frontmatter에 trigger_type 필드 추가(스팟 산출 시 manual). 봇이 pending에 frontmatter 포함 md 직접 출력. "저장 후 [ESCALATE] 사장님 직접" → "[CALL:관리팀장] 결재요청"으로

### ② [폐기] line 237 훅 — 불필요
봇 Write는 Claude CLI 내부에서 발생, agent.js는 최종 텍스트만 받음. line 237 훅으로 "저장 직후" 감지 불가.
대신 방법 X 채택: 봇이 응답에 [CALL:관리팀장] 포함 → 기존 line 249 [CALL:] 프로토콜이 관리팀장에게 전달. 신규 훅 없음.

### ③ 방식 P 확정 — agent.js 코드 변경 0곳
[중요] reviewByManager를 콘텐츠봇 인스턴스에서 함수로 돌리면 systemPrompt=content/SKILL.md라 관리팀장이 콘텐츠봇 SKILL로 검토하는 오류. 폐기.
방식 P: [CALL:관리팀장]을 디스코드 채널 메시지로 전송 → 진짜 관리팀장 봇 인스턴스(opus, admin/SKILL.md)가 수신 → 자기 SKILL로 검토. 기존 [CALL:] 배관 재사용, agent.js 신규 코드 없음.
관리팀장은 opus+Bash도구 보유 → mv(pending→approved)도 관리팀장이 직접 실행. agent.js renameSync 안 건드림.

## 최종 구현 = SKILL.md 2개만 수정 (agent.js 0곳)
### A. content/SKILL.md 저장규칙
- frontmatter에 trigger_type 추가(스팟=manual)
- 봇이 pending에 frontmatter 포함 md 직접 저장
- "저장 후 [ESCALATE] 사장님 직접" → "응답에 [CALL:관리팀장] 결재요청 {파일경로} 포함"

### B. admin/SKILL.md (관리팀장)
- "[CALL:]로 결재요청 받으면: 해당 pending 파일 읽고 품질검토 → 승인이면 mv pending→approved + 채널 알림 / 고위험이면 [ESCALATE] 채널 사장님 멘션"
- 정기(schedule) 폴링은 발동 주체 생긴 뒤 추가 (이번 범위 밖)

### ④ 정기 폴링 stub — setInterval 자리만
- 발동 주체 생기면 채울 TODO. 이번 세션 구현 안 함.

## 제약 / 미결
- str_replace old/new는 위 line의 실제 원문으로 작성(공백·들여쓰기 정확히). 화면캡처 기반이라 1~2회 매칭 왕복 가능.
- 정기 경로 실증 불가(발동 주체 없음) → 스팟만 디스코드 실증.
- backup: src/agent.js 편집 전 cp 백업 필수.

## [최종] str_replace 적용안 (다음 세션 이것만 적용)
### [A] content/SKILL.md OLD(line 129~131) → NEW
NEW:
- 프론트매터 필수: source_cycle([[{내에이전트명}/raw/cycles/{프로젝트}/cycle-NNN-{슬러그}]]), trigger_type(스팟=manual / 정기=schedule)
- 저장 후 반드시 응답에 `[CALL:관리팀장] 결재요청 {저장한 파일의 절대경로}` 포함 (사장님 직접 ESCALATE 금지)
- 승인→approved/ 이동 | 반려→rejected/ 이동 (관리팀장 처리, 고위험만 사장님 ESCALATE)

### [B] admin/SKILL.md OLD(line 19~22) → NEW
[주의] 분기는 위험도 기준(파트C §8). trigger_type 아님. trigger_type은 검토시점(즉시/배치)만 가름.
NEW:
- 검토 항목: 누락, 팩트 오류, 포맷 불일치, 다른 에이전트 산출물과 중복, trigger_type 확인
- 저위험(routine 품질 OK) → 직접 승인: `mv {파일경로} {approved경로}` 후 채널에 ✅승인 알림
- 고위험(정책·민감·판단 필요) → 검토 의견 포함해 [ESCALATE] 사장님에게 전달
- 수정 필요 → 해당 에이전트에게 [CALL:]로 수정 요청
- trigger_type=schedule(정기)는 여러 건 모아 배치 검토 (발동주체 없어 이번 미실증, 규칙만 박음)

### 적용 순서
1. cp 백업 (SKILL 2개)
2. grep -c로 old_str 유일성 확인(각 1건) 후 str_replace
3. broker 재기동: launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker
4. 디스코드 실증: 콘텐츠봇에 스팟 작업 1건 → 봇이 pending 저장+[CALL:관리팀장] → 관리팀장이 검토 → 저위험이면 mv 승인(✅) / 고위험이면 ESCALATE. cycle 1바퀴 확인.
