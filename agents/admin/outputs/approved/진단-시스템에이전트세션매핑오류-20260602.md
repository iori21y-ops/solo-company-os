# 진단 보고서 — 시스템 에이전트 "세션 ID 미존재" 오류

- 작성: 관리팀장
- 일자: 2026-06-02
- 트리거: 대표님 지시 ("결재 요청한 사안 직접 확인하고 진행")
- 대상 오류: `시스템` 에이전트가 기획팀장 멘션 처리 중 "세션 ID `1510181005330350100`에 해당하는 세션이 존재하지 않습니다" 반환

## 1. 결론 (요약)
**라우팅 테이블/세션 매핑 손상 아님.** 세션 저장소는 정상. 원인은 `시스템`(sysadmin) 에이전트의 **도구 오용**이다.
**조치 필요한 인프라 수정 없음.** 운영 규칙 정립으로 종결.

## 2. 근본 원인 (도구 호출 trajectory로 확정)
- `시스템` 에이전트 = OpenClaw `sysadmin` 계정, 모델 **ollama/qwen3:8b** (로컬 8B 경량 모델, bot ID 1511003688003567939)
- `기획팀장` = OpenClaw `default` 계정 = GPT 기반 (bot ID 1510158572107989064)
- `1510181005330350100` = **#컨텐츠-제작소 채널 ID** (세션 키 아님)
- 세션 인덱스(`agents/sysadmin/sessions/sessions.json`)에 해당 채널 세션은 **정상 존재**:
  - 키 `agent:sysadmin:discord:channel:1510181005330350100` → 내부 sessionId `49241c8c-8fbc-495e-acb0-a1bad51df0b1`
- **실제 실패한 도구 호출 (trajectory.jsonl seq=5 확정):**
  ```json
  sessions_send(
    agentId: "1511005977607012426",     // ← 기획팀장 아님. 이건 '시스템' 역할 ID
    sessionKey: "1510181005330350100",   // ← 채널 ID. 올바른 키는 agent:sysadmin:discord:channel:1510181005330350100
    message: "기획팀장, 시스템이 멘션을 받았습니다. 확인해 주세요."
  )
  → {"status":"error","error":"No session found: 1510181005330350100"}
  ```
- 3중 오류 (모두 모델 측 판단 실패):
  1. **명시 지시 위반** — 대표님이 직전 메시지에서 "sessions 말고 **디스코드 멘션으로**"라고 못박았으나 qwen3:8b가 무시하고 `sessions_send` 사용
  2. **sessionKey 오용** — 내부 세션 키 대신 디스코드 채널 ID 전달 → "No session found"
  3. **agentId 오용** — 기획팀장이 아닌 '시스템' 역할 ID를 타깃으로 지정
- 결론: 인프라/세션 손상 아님. **약한 8B 모델의 도구 환각**이 단일 원인.

## 3. 부수 점검 결과 (정상)
- ollama: 가동 중(PID 23015), qwen3:8b 로드 정상. (과거 로그의 `ollama: command not found`는 PATH 미등록 시점의 이력, 현재 `/usr/local/bin/ollama` 심볼릭 정상)
- ai.openclaw.gateway: 가동(PID 61103)
- com.rentailor.claude-broker: 가동(관리팀장/마케팅/콘텐츠/데이터 로그인 정상)
- 세션 매핑/바인딩(openclaw.json `bindings`): 정상

## 4. 영향도
- **비차단(non-blocking).** `시스템→기획팀장` 직접 전달만 실패. 운영상 우회로 이미 존재:
  - 관리팀장(나)은 기획팀장과 디스코드 멘션으로 정상 소통 → **나의 직무 #5 (기획팀장 연결 창구)** 그대로 작동
  - 즉, 시스템↔기획팀장 통신은 관리팀장 경유로 처리하면 됨

## 5. 조치 / 권고
1. (즉시·완료) 본 건은 인프라 손상 아님 → 라우팅 점검·시스템 자가진단 지시 불필요. 종결.
2. (운영 규칙) `시스템`(qwen3:8b)은 GPT 기반 기획팀장에 `sessions_send`로 직접 전달 금지. 교차 통신은 **관리팀장 경유**로 일원화.
3. (권고·대표 판단) 재발 방지를 위해 sysadmin 에이전트의 교차세션 도구(`sessions_send`/`sessions_spawn`) 사용 범위를 제한하거나, 시스템 모델을 더 신뢰도 높은 모델로 상향하는 안 검토. → 게이트웨이 설정 변경이라 대표 승인 후 집행.
