# 수정안 — 새벽 점검 스크립트 9722 상시감시 제외 (A안)

- **작성:** 관리팀장 / 2026-06-02
- **대상 파일:** `~/projects/_meta/scripts/server-health-check.sh` (launchd 매일 07:00 실행)
- **승인 근거:** 사장님 A안 결정 (2026-06-02, 관리팀장실)
- **위험도:** 하 — 알림 오탐 제거. 서비스 로직·DB·외부 호출 영향 없음. 라이브 모니터링 스크립트라 수정 전 git commit 후 집행.

---

## 1. 문제 (확정 사실)
- 9722 가격크롤러는 **매월 1일 02:00 1회 실행 후 스스로 종료**되는 배치 작업 (launchd `매월 1일`, `KeepAlive=false`, `run_monthly.sh`가 수집 후 서버 kill).
- 현재 8번 블록은 이를 **상시 서버로 오인**하고 매일 07:00 "9722 무응답"을 발송 → **매일 오탐**.
- 9722를 호출하는 활성 n8n 워크플로우 0개 → 꺼져 있어도 실제 장애 아님.
- 6/1 02:00~02:08 수집 정상 완료 (로그 마지막 `=== 월간 가격표 수집 끝 ===`, mtime 06-01).

## 2. 변경 내용
**Before (8번 블록 후반부):**
```bash
if ! curl -s -o /dev/null -w '' --max-time 5 http://localhost:9722 > /dev/null 2>&1; then
  PROBLEMS="${PROBLEMS}🟡 Flask가격크롤러(9722) 포트 무응답\n　→ 수동 확인: cd ~/projects/cadam && python3 price_crawler.py\n"
fi
```

**After (포트 상시감시 삭제 → 월 1회 수집 성공 검증으로 대체):**
```bash
# --- 8-1. 월간 가격크롤러 수집 성공 검증 (매월 2~3일에만) ---
#   [2026-06-02 A안] 9722는 매월 1일 1회 실행 후 종료되는 배치 작업이므로 상시 포트 감시 제외.
#   대신 '이번 달 수집이 성공했는지'를 1일 다음(2~3일)에만 1회 검증한다.
DOM=$(date '+%d')
if [ "$DOM" = "02" ] || [ "$DOM" = "03" ]; then
  CRAWLER_LOG="$HOME/projects/cadam/cadam-n8n/price_crawler/crawler.log"
  THIS_MONTH=$(date '+%Y-%m')
  if [ ! -f "$CRAWLER_LOG" ]; then
    PROBLEMS="${PROBLEMS}🔴 가격크롤러 로그 없음 (이번 달 수집 미실행 의심)\n　→ 수동 실행: bash ~/projects/cadam/cadam-n8n/price_crawler/run_monthly.sh\n"
  else
    LOG_MONTH=$(date -r "$CRAWLER_LOG" '+%Y-%m')
    if [ "$LOG_MONTH" != "$THIS_MONTH" ] || ! tail -5 "$CRAWLER_LOG" | grep -q "월간 가격표 수집 끝"; then
      PROBLEMS="${PROBLEMS}🔴 이번 달(${THIS_MONTH}) 가격크롤러 월간 수집 실패/누락\n　→ 수동 실행: bash ~/projects/cadam/cadam-n8n/price_crawler/run_monthly.sh\n"
    fi
  fi
fi
```

> n8n(5678) 포트 체크는 그대로 유지. 9722 줄만 제거하고 위 8-1 블록을 추가한다.

## 3. 효과
- 매일 오탐 종료 (29~30일/월 헛알림 제거).
- 진짜 중요한 것 — **월 1회 수집이 실제로 성공했는지** —를 매월 2~3일 단 1회만, 로그 마커+mtime 이중 검증으로 잡아낸다.
- 수집 실패 시에는 **올바른 복구 명령**(`run_monthly.sh`, 경로/`--serve` 문제 없음)을 안내. 기존 잘못된 경로 명령 폐기.

## 4. 집행 절차 (승인 후)
1. `git -C ~/projects/_meta add -A && git commit -m "checkpoint: 점검스크립트 9722 상시감시 제외 전"`
2. 8번 블록 9722 줄 제거 + 8-1 블록 추가
3. `bash -n server-health-check.sh` 문법 검증
4. 수동 1회 실행해 오탐 사라짐 확인 후 채널 보고
