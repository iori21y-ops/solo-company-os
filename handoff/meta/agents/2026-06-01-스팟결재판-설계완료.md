# 핸드오프 — 스팟 결재판 설계 완료 → 다음: str_replace 적용 + 디스코드 실증

- area: meta/agents (디스코드 구현 트랙)
- date: 2026-06-01
- repo: ~/projects/_meta

## 1. 이어가는 중
9-에이전트 OS 디스코드 구현. 스팟 결재판 설계 완료, 합의메모 박제 끝. 다음 세션은 설계대로 str_replace 적용 + 디스코드 실증만. 받으면 ①conversation_search로 이번 세션(스팟 결재판 설계) 검토 ②governance 합의메모 read ③추측 반박 금지, 맥락 파악 우선.

## 2. 마스터플랜
클코(유료·사람감독)가 미션 정의 → 로컬AI/n8n 위임. 결재 골격(파트C §8): 실무봇 산출→pending→관리팀장 품질검토→저위험=관리팀장 승인/고위험=대표 ESCALATE→approved. 관리팀장=opus, 실무봇=sonnet.

## 3. 기존 자산 (새 세션 첫 단계로 read)
- ★합의메모(이번 박제): ~/projects/_meta/governance/합의메모-스팟결재판-구현설계-20260601.md. 맨 끝 "[최종] str_replace 적용안" 섹션에 [A][B] old/new + 적용순서 4단계 전부 있음. 이것만 보면 바로 적용 가능.
- 파트C 합의메모: governance/합의메모-파트C-협업결재학습모델.md (§8 결재 골격).

## 4. 이번 세션 결론 (확정 설계)
방식 P 확정 — **agent.js 코드 변경 0곳**. SKILL.md 2개만 수정.
- 왜 코드 0곳: [CALL:관리팀장]을 디스코드 채널 메시지로 보내면 진짜 관리팀장 봇 인스턴스(opus, admin/SKILL.md)가 자기 SKILL로 수신·검토. 기존 [CALL:] 배관 재사용. 관리팀장이 opus+Bash라 mv(pending→approved)도 직접 실행.
- [A] content/SKILL.md: 봇이 pending에 frontmatter(trigger_type:manual) 포함 직접 저장 + 응답에 [CALL:관리팀장] 결재요청 {파일경로}. 사장님 직접 ESCALATE 제거.
- [B] admin/SKILL.md: [CALL:] 결재요청 받으면 pending 파일 품질검토 → 저위험=직접 mv 승인 / 고위험=ESCALATE / 수정필요=[CALL:]로 해당 에이전트.

## 5. 판단 변경 (추론 노출)
이번 세션 설계가 3번 뒤집힘 — 다음 세션 클코가 같은 실수 반복 않게 기록:
- (1) line 237 이벤트 훅 폐기: 봇 Write는 Claude CLI 내부 발생, agent.js는 최종 텍스트만 받아 "저장 직후" 감지 불가.
- (2) reviewByManager 함수(콘텐츠봇 인스턴스 내 실행) 폐기: systemPrompt=content/SKILL.md라 관리팀장이 콘텐츠봇 SKILL로 검토하는 오류. → 방식 P(채널 메시지로 진짜 관리팀장 봇 호출)로.
- (3) 분기 기준: trigger_type 아님(클코가 manual→ESCALATE로 잘못 짰던 것). 파트C §8대로 **위험도**(저위험=관리팀장 승인/고위험=ESCALATE). trigger_type은 검토시점(즉시/배치)만 가름.

## 6. 우선순위/범위 (확정)
- 이번 세션: 스팟(manual) 경로만 cycle 1바퀴. 정기(schedule)는 발동 주체(봇 깨우는 schedule 트리거)가 없어 실증 불가 → SKILL에 규칙만 박고 미실증.
- 정기 발동 주체 만들기는 별도 파트(후속). n8n GWP는 네이버/WP발행·텔레그램으로 가지 디스코드 봇 안 깨움. 봇은 사장님 수동 멘션만 발동.

## 7. 새 세션 첫 지시 (순서대로)
1. conversation_search로 이번 세션(스팟 결재판 설계) 검토 + 합의메모 read (특히 맨 끝 [최종] 섹션).
2. cp 백업: content/SKILL.md, admin/SKILL.md 각각 .bak-HHMM.
3. grep -c로 [A][B] old_str 유일성 확인(각 1건) → str_replace 적용. [A]=content/SKILL.md line129~131, [B]=admin/SKILL.md line19~22. new는 합의메모 [최종] 섹션 그대로.
4. broker 재기동: launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker
5. 디스코드 실증: 콘텐츠봇에 스팟 작업 1건 지시 → 봇이 pending 저장+[CALL:관리팀장] 출력 → 관리팀장이 검토 → 저위험이면 mv 승인(✅ 알림)/고위험이면 ESCALATE. cycle 1바퀴 확인.

## 제약
- 클코 자율실행 금지(명령어 문서로만), 적용 전 코드 읽기 필수, 모름은 추측 금지.
- str_replace만(통째 덮어쓰기 금지) / 한 세션=한 파트 / 합의메모 없으면 박제 안 함 / git 자동커밋·푸시 금지(명시 시만).
- 답변 단일 사안 / 결정 합의 후 재검토 금지(새 외부 사실 때만) / 비개발자 이해 가능하게.
- n8n 수정 안 함(참조만) / second-brain vault 읽기만 / 클코 모델 sonnet 기본·opus 어드바이저.

## 환경
- broker=~/.openclaw/workspace/claude-broker (src/agent.js), 재기동=launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker. 백업 src/agent.js.bak-* (이번 세션 코드 미수정, agent.js 변경 0곳).
- 봇ID: 관리5738/기획9064/마케팅8104/콘텐츠2535/데이터6609, 채널1510306207573741719.
- SKILL 경로: ~/projects/_meta/agents/{content,admin}/SKILL.md. AGENTS_ROOT=~/projects/_meta/agents.
- agent.js 구조지도는 합의메모 본문에 박제됨(line 112결재판/188저장/237파싱/239승인마킹/249 CALL처리/302 askClaude/521 parseCallProtocol).
- 관리팀장=opus, Bash도구 보유. pending=AGENTS_ROOT/{에이전트}/outputs/pending.
