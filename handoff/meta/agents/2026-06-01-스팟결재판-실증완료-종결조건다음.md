# 핸드오프 — 스팟 결재판 적용+실증 완료 → 다음: cycle 종결조건+범위제한 설계

- area: meta/agents (디스코드 구현 트랙)
- date: 2026-06-01
- repo: ~/projects/_meta

## 1. 이어가는 중
9-에이전트 OS 디스코드 구현. 스팟 결재판 SKILL 적용 완료 + 디스코드 cycle 1바퀴 실증 성공. 다음 세션은 "cycle 종결조건+범위제한" 설계(새 파트). 받으면 ①conversation_search로 이번 세션(스팟 결재판 실증) 검토 ②추측 반박 금지, 맥락 파악 우선 ③아래 4번 실증결과·5번 발견문제부터 read.

## 2. 마스터플랜
클코(유료·사람감독)가 미션 정의 → 로컬AI/n8n 위임. 결재 골격(파트C §8): 실무봇 산출→pending→관리팀장 품질검토→저위험=관리팀장 직접 mv 승인/고위험=대표 ESCALATE→approved. 관리팀장=opus, 실무봇=sonnet.

## 3. 기존 자산
- 스팟 결재판 합의메모: ~/projects/_meta/governance/합의메모-스팟결재판-구현설계-20260601.md (적용 완료분).
- 파트C 합의메모: governance/합의메모-파트C-협업결재학습모델.md (§8 결재골격).
- SKILL: ~/projects/_meta/agents/{content,admin}/SKILL.md. 백업 .bak-2116.
- broker=~/.openclaw/workspace/claude-broker (src/agent.js). 재기동=launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker. 이번 트랙 agent.js 코드 0곳 변경.
- 봇ID: 관리5738/기획9064/마케팅8104/콘텐츠2535/데이터6609. 채널1510306207573741719.

## 4. 이번 세션 완료 (확정)
(A) str_replace 적용 완료: content/SKILL.md([ESCALATE]제거→[CALL:관리팀장], frontmatter에 source_cycle+trigger_type), admin/SKILL.md(저위험=mv승인/고위험=ESCALATE/수정=CALL/schedule규칙). 백업 .bak-2116, git diff 2 files changed 21+/10-. 커밋 안 함.
(B) broker 재기동 성공(state=running, pid=33754).
(C) 디스코드 스팟 실증 cycle 1바퀴 성공: 대표님 지시→콘텐츠봇 카드뉴스 초안+pending저장+[CALL:관리팀장]→관리팀장 검토(단정표현 과장광고소지 발견)→[CALL:콘텐츠]수정요청→콘텐츠 v2→관리팀장 저위험판정 직접 mv 승인. ★핵심: 사장님 ESCALATE 없이 관리팀장이 직접 승인=목표달성. 수정필요=CALL 경로도 실증. 품질게이트 실작동(과장광고 완화 강제).

## 5. 발견 문제 = 다음 파트
승인(approved 이동) 후 cycle 종료 안 됨, 봇 핑퐁:
- 콘텐츠봇: 승인 후에도 index.md 등록 + 같은 파일 재결재 + pending 잔여 다른 파일(speccard-avante 2건) 자발적 결재요청 + 데이터검증 일정 질문까지 계속 새 액션 생성.
- 관리팀장: 수신하면 자동 응답 → 콘텐츠봇 재기동 → 핑퐁.
- broker "봇간 대화 5회 도달" 차단이 막아줌(무한루프 방지 확인, 안전장치 작동).
- 근본원인: "승인=cycle종료" 종결조건 + "이번 요청 1건만" 범위제한이 양쪽 SKILL에 없음. 한쪽이 멘션 멈춰야 끊김.

## 6. 우선순위/범위 (다음 세션)
- 다음 파트: cycle 종결조건+범위제한 설계 1건. SKILL 수정(합의메모 먼저 작성·합의 후 str_replace).
- 설계 방향(미합의, 다음 세션 검토): ①콘텐츠봇=결재요청 당 작업 1건만, 승인/반려 응답 받으면 멈춤(추가 [CALL]·재멘션 금지), pending 잔여건 자발적 결재 금지 ②관리팀장=승인/반려 처리 후 종결마커 출력+추가 멘션 금지, 중복 결재요청은 1회 안내 후 멈춤. 핵심=한쪽이 멘션 멈추면 핑퐁 끊김.
- 미실증(후속): 고위험 ESCALATE 경로(이번엔 저위험만), 정기(schedule) 경로(발동주체 부재).
- 관찰: admin/SKILL §3 결재집행에 "사장님 [승인] 지시→mv" 남아있음. 이번 실증선 충돌 안 한 듯(관리팀장이 §2 저위험 직접승인 따름). 종결조건 설계 때 §2와 역할 명확화 같이 볼 것.

## 7. 새 세션 첫 지시 (순서대로)
1. conversation_search로 이번 세션(스팟 결재판 실증) 검토.
2. content/SKILL.md, admin/SKILL.md 현재 상태 read — 봇이 승인 후 멈추는 규칙이 어디에도 없음을 직접 확인.
3. broker agent.js의 "봇간 5회 차단" 로직 위치 grep으로 확인(종결조건을 SKILL로 둘지 코드로 둘지 판단용, 추측 금지).
4. 종결조건+범위제한 설계안 합의메모 작성(미합의 상태로 시작, 사장님과 한 사안씩 확정).
5. 합의되면 str_replace 적용 → broker 재기동 → 디스코드 재실증(스팟 1건, 이번엔 승인 후 멈추는지 확인).

## 제약
- 클코 자율실행 금지(명령어 문서로만), 적용 전 코드 읽기 필수, 모름은 추측 금지.
- str_replace만(통째 덮어쓰기 금지) / 한 세션=한 파트 / 합의메모 없으면 박제 안 함 / git 자동커밋·푸시 금지(명시 시만).
- 답변 단일 사안 / 결정 합의 후 재검토 금지(새 외부 사실 때만) / 비개발자 이해 가능하게.
- n8n 수정 안 함(참조만) / second-brain vault 읽기만 / 클코 모델 sonnet 기본·opus 어드바이저.
