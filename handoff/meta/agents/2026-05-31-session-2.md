# 핸드오프 — 파트B 결재판 코드 구현 (구현 착수)

- area: meta/agents (claude-broker)
- date: 2026-05-31

## 1. 이어가는 중
9-Agent OS 디스코드 구현 트랙. 직전 세션에서 **REG-001 결재·보고 규정 제정·검토·저장 완료** + **파트B(결재판 코드) 합의메모 확정**. 이번 세션은 **파트B 코드 구현 착수**.
**받으면 먼저:** ① `conversation_search`로 이전 대화 검토 ② 추측 기반 반박부터 하지 말 것.

## 2. 마스터플랜 — 3계층 강제 모델
규정을 에이전트에 적용하는 방식:
- **1계층 (코드):** 가장 강력. `agent.js`가 파일 이동·권한을 물리적으로 강제. ← **파트B가 이것.**
- **2계층 (프롬프트):** 각 에이전트 지시문에 "자기 몫"만 컴파일.
- **3계층 (문서):** REG-001 전문은 사람용·분쟁용. 평소 주입 안 함.
핵심: 에이전트는 규정문서를 안 읽는다. 규정 = "코드 한 곳 + 프롬프트 한 줄"로 번역. REG-001 9조 매핑이 어디 박는지 기록.

## 3. 기존 자산
- 규정: `~/projects/_meta/governance/REG-001-결재보고프로세스.md` (저장 완료) + `index.md`(SSoT)
- 합의메모: `합의메모-파트B-결재판코드연결.md` (설계 확정)
- 코드: `~/.openclaw/workspace/claude-broker/src/agent.js` (419줄), `agents.config.js`
- **시스템 2개 (중요):**
  - 관리팀장 + 실무봇 = claude-broker(Claude). `agents.config.js` skillPath → SKILL.md → `--system-prompt` 주입. 현재 4봇: 관리팀장/마케팅/콘텐츠/데이터.
  - 기획팀장 = OpenClaw(gpt-5.5). 지시문 = `~/.openclaw/workspace/IDENTITY.md`(응답 규칙) + `SOUL.md`(역할).
- **agent.js 구조 지도:** 65 messageCreate / 78~82 모델커맨드 / 85~110 복구승인 / 112~130 봇루프제한 / 132~136 권한 / 140~145 Claude호출 / **149~154 `[승인]/[반려]` 봇 프로토콜(결재판과 별개!)** / 183 extractPrompt

## 4. 작업 흐름 / 1차 결과 (직전 세션 산출)
- REG-001 제정 + 검토 4건 반영: 교착해소(관리팀장 우선) / 외부배포 일일보고 가시성 / 2차수정 Phase2 복귀 / 9조 두 시스템 매핑 정정
- governance/ 저장 완료
- 파트B 합의메모 확정

## 5. 판단 변경 추론
- 원래 핸드오프 파트B = 2판정(승인/반려) + 사장님 승인. **규정 검토로 확장:** 3판정(승인/수정/반려), 승인 주체 업무유형별(정기=팀장 / 스팟=대표).
- 파트B는 **기본 집행만.** phase 라우팅·업무유형 자동판별은 frontmatter `phase` 상태값 설계가 선행돼야 하므로 **파트C로 분리.**

## 6. 우선순위 / 범위
- **파트B 포함:** 대기목록 / 승인(→approved) / 수정(→revision + feedback) / 반려(→rejected) + 디스코드 보고
- **파트C 제외:** phase 인식 수정 라우팅, 업무유형별 승인주체 자동판별, 미등록 6에이전트(strategy/sales/cs/ops/finance/legal)

## 7. 구체적 첫 지시
1. 복구 승인 핸들러 펼치기: `sed -n '85,110p' ~/.openclaw/workspace/claude-broker/src/agent.js` — **이걸 템플릿으로** 동일 스타일 핸들러 설계.
2. 4개 명령(대기목록/승인/수정/반려) `str_replace` 편집안 작성. 삽입 위치 = **110줄 뒤, 112줄 앞.** 처리 후 즉시 `return`.
3. import 추가안: `node:fs`의 `renameSync`, `mkdirSync`.
4. 권한: **대표(사람) 또는 관리팀장 봇 ID만** 실행. 그 외(실무봇 포함) 차단.
5. `revision/` 폴더 + `[파일명].feedback.md` 사이드카.

## 제약 (반드시 준수)
- 코드 통째 덮어쓰기 금지 — `str_replace`로만
- 클코 자체 진행 금지 — 명령어 문서로만
- git 자동 커밋 금지 (명시 지시 시에만)
- 한 세션 = 한 파트
- 합의메모 없으면 박제 안 함
- second-brain vault 읽기만
- 모름은 추측 금지 — 못 보는 정보는 "확인하려면 [명령어]" 형식
- **149~154 봇 `[승인]/[반려]` 프로토콜은 결재판과 별개 — 건드리지 말 것**
