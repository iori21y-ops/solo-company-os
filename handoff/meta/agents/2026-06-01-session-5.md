# 핸드오프 — 타임아웃 해결 완료(Task차단) → 다음: SKILL 저장규칙 수정(봇 직접저장·ESCALATE 우회 제거)

- area: meta/agents (디스코드 구현 트랙)
- date: 2026-06-01
- repo: ~/projects/_meta (github.com/iori21y-ops/solo-company-os)

## 1. 이어가는 중
9-에이전트 OS 디스코드 구현 트랙. 연속. 받으면 ①conversation_search로 이번+직전 세션 검토 ②추측 기반 반박 금지. 이번 세션은 도구사용 복잡작업 타임아웃을 해결 완료. 다음은 봇이 저장입구·관리팀장을 우회하는 SKILL 저장규칙 수정.

## 2. 이번 세션 완료(타임아웃 해결)
원인을 추측 않고 재현으로 가름. 테스트1~4로 분리: CLI단독은 도구허용해도 36초 정상 → broker 차이는 --system-prompt(SKILL.md) 주입. SKILL 주입 시 sonnet봇이 Task 도구로 opus 서브에이전트를 자율 spawn(38,907토큰, 9턴, 234초) → 300초 초과. hang(나) 아니라 진행중(가)이되 opus spawn이 시간 대부분 차지. 비용: OAuth 구독이라 cost필드($0.41)는 표시값일뿐 실제 추가청구 0(단 구독 한도는 소진). 해결=agent.js runClaudeCli args에 --disallowedTools Task 1줄 추가(347줄 --dangerously-skip-permissions 직후), 전체봇 공통. 관리팀장은 Claude Task 도구 안 씀([CALL:]태그=broker처리라 무관) 확인 → 전체차단 안전. 재기동 후 디스코드 실증: 아반떼 스펙카드가 300초 타임아웃 없이 완료, PNG 1080x1350 생성, verdict게이트 통과, ESCALATE 발송 ✅. 백업=src/agent.js.bak-0333.

## 3. 다음 파트(SKILL 저장규칙 수정) — 핵심
Task 차단 후에도 봇이 Write 도구로 outputs/pending/에 .md+.png(219KB) 실제 저장 확인. 즉 봇이 저장입구(@관리팀장 저장 코드)를 우회해 직접 디스크에 씀. ESCALATE도 사장님께 직접 발송(스크린샷). 경년님 지적="관리팀장에게 줘야 하는 것". 합의된 방향: content봇은 초안 텍스트 출력까지만, 저장은 저장입구가, 결재는 관리팀장(opus) 경유. 사장님 직접 ESCALATE 아님.

## 4. 다음 파트 작업 대상(원문 확인됨)
content/SKILL.md 126~133줄 "## 산출물 저장 규칙" 블록:
- 127줄: 내 산출물 저장 ~/.../outputs/pending/
- 130줄: 저장 후 반드시 [ESCALATE]로 사장님에게 검토 요청
주의: 이 블록은 {내 에이전트명} 변수 쓰는 9에이전트 공통규칙. source_cycle 프론트매터(학습루프 추적성)·다른 에이전트 approved/ 참조는 살려야 함. 단순 줄삭제 아니라 "봇 직접저장·직접ESCALATE 제거하되 추적성·참조는 보존"이라 신중. 한 세션=한 파트라 이번에 안 묶고 분리한 이유.

## 5. 다음 파트 첫 지시
1. conversation_search로 이번 세션(타임아웃·Task차단) + verdict게이트 세션 검토.
2. SKILL 저장규칙이 content봇만인지 9에이전트 공통인지 확정: grep -n "산출물 저장 규칙\|ESCALATE\|pending" ~/projects/_meta/agents/*/SKILL.md.
3. 봇이 실제 저장한 파일 재확인: ls -la ~/projects/_meta/agents/content/outputs/pending/speccard* (.md+.png 0337생성분).
4. 저장입구 코드가 봇 출력을 어떻게 받아 저장하는지 재확인(agent.js @관리팀장 저장 핸들러) → 봇이 직접 안 써도 저장입구로 들어오는 흐름이 성립하는지.
5. 합의메모 초안(str_replace old/new 명시) → 경년님 합의 → str_replace → 디스코드 재테스트(봇이 직접저장·직접ESCALATE 안 하고 텍스트 초안만, 저장은 @관리팀장 저장으로 일원화 확인).

## 6. 제약
- 클코 자체 진행 금지(명령어 문서로만), 적용 전 코드검토 필수.
- 통째 덮어쓰기 금지(str_replace·신규생성만) / 한 세션=한 파트 / 합의메모 없으면 박제 안 함 / git 자동커밋·푸시 금지.
- n8n 절대수정 안 함(참조) / second-brain vault 읽기만.
- 모름은 추측 금지 — "확인하려면 [명령어]".
- SKILL은 9에이전트 공통규칙이라 신중 — 추적성(source_cycle)·참조 보존하며 봇 직접저장·직접ESCALATE만 제거.
- 답변 단일 사안 / 결정 합의 후 재검토 금지.

## 7. 환경
- broker=~/.openclaw/workspace/claude-broker (src/agent.js), 재기동=launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker
- 봇ID: 관리5738/기획9064/마케팅8104/콘텐츠2535/데이터6609, 채널1510306207573741719
- .env CLAUDE_TIMEOUT_MS=300000(이번에 안 건드림. Task차단으로 충분) / 적용된 변경=--disallowedTools Task (agent.js 347줄)
- verdict게이트=agent.js 314~333줄 readdirSync/readFileSync(node fs, Claude도구 아님). verdict경로 data/outputs/{pending,approved,rejected}
- 발행=marketing/scripts/wp_publish.py / 모델: 클코 기본 sonnet, opus 어드바이저 / 관리팀장 opus
- 백업: src/agent.js.bak-0333
