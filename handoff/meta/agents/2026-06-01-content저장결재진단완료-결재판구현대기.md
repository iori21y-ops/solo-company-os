# 핸드오프 — content 저장·결재 진단 완료(합의메모 박제) → 다음: 관리팀장 결재판 구현(파트①)

- area: meta/agents (디스코드 구현 트랙)
- date: 2026-06-01
- repo: ~/projects/_meta (github.com/iori21y-ops/solo-company-os)

## 1. 이어가는 중
9-에이전트 OS 디스코드 구현 트랙. 연속. 받으면 ①conversation_search로 이번 세션(content 저장결재 진단)+파트C 협업결재 합의 검토 ②추측 기반 반박 금지, 맥락 파악 우선. 이번 세션은 content 저장·결재 흐름을 진단하고 합의메모로 박제 완료. 다음은 관리팀장 결재판 구현(파트①).

## 2. 마스터플랜
클코(유료·사람감독)가 미션 정의 → 로컬AI/n8n(무료·자율) 위임. 9에이전트 디스코드 OS가 그 진화형. 결재 골격(파트C §8): 실무봇 산출 → outputs/pending/ → 관리팀장 1차 품질검토 → 정기·저위험=관리팀장 승인 / 스팟·고위험=대표 ESCALATE → approved/. 관리팀장=opus(조율·품질검조·결재집행), 실무봇=sonnet.

## 3. 기존 자산
- 합의메모(이번 박제): ~/projects/_meta/governance/합의메모-content저장결재흐름-진단순서확정-20260601.md (2605 bytes). 진단·순서·트리거 추천 전부 기록됨. 새 세션 첫 단계로 이걸 read.
- 파트C 합의메모: governance/합의메모-파트C-협업결재학습모델.md (§8 공통 결재 골격).
- 직전 적용 변경: agent.js 347줄 --disallowedTools Task (타임아웃 해결). 백업 src/agent.js.bak-0333.

## 4. 이번 세션 진단 (코드 실측 src/agent.js)
- 저장·승인·반려·대기 = 전부 사장님 전용 수동 디스코드 명령(!message.author.bot). AGENT_BOT_IDS(line 177)=content/marketing/data 3개만 저장 가능. 저장→봇 최근 메시지를 pending/에 writeFileSync(line 194). 승인/반려→renameSync pending→approved/rejected(line 157).
- ESCALATE: 봇 응답 [ESCALATE] → line 521 파싱 → line 262 사장님 DM. line 329/331: 데이터 정합성 verdict 없으면 봇에게 강제 ESCALATE 지시.
- 관리팀장 자동 결재판 = 없음(setInterval/watchPending 없음). 결재판 커맨드 line 112만 있고 사장님 수동 실행.

## 5. 판단 변경 (추론 노출)
직전 세션은 봇이 pending에 Write로 직접 저장하는 것을 저장입구 우회=버그로 봤으나, 파트C §8 첫 문장이 실무봇 산출→pending/이라 봇 pending 저장은 설계대로 정상. 직전 진단이 오판. 진짜 문제 둘: ①봇이 정기 업무인데 대표에게 직접 ESCALATE(관리팀장 건너뜀) ②관리팀장 결재판(정기 승인) 부재. 그래서 SKILL만 먼저 고치면(직접 ESCALATE 제거) 받을 결재판이 없어 pending에 쌓이기만 함. 순서를 받는 쪽 먼저로 뒤집음.

## 6. 우선순위/범위 (확정)
- 파트①(다음): 관리팀장 결재판 구현 — 관리팀장이 pending 감시→정기 품질검토·승인 / 스팟은 대표 ESCALATE 유지.
- 파트②(그 다음): SKILL 저장규칙 수정 — 봇 직접 대표 ESCALATE 제거, 정기는 관리팀장 경유. pending 저장·frontmatter(source_cycle) 추적성은 보존.
- 트리거 방식: 클코 추천=(b) 사장님 명령 트리거→관리팀장 opus가 pending 품질검토 후 승인/ESCALATE 분기. (a)자동폴링은 opus 반복호출=구독 한도 소진이라 비추. 경년님 최종 확정은 새 세션 첫머리에 받을 것.

## 7. 새 세션 첫 지시 (3~5)
1. conversation_search로 이번 세션(content 저장결재 진단)+파트C 검토. governance 합의메모 2종 read.
2. 결재판 트리거 (a)/(b) 경년님께 확정 요청(클코 추천 b). 확정 전 코드 변경 금지.
3. 결재판 관련 코드 읽기 전 확인: line 112 결재판 커맨드 본체, line 262 ESCALATE DM 발송부, 관리팀장 봇 호출(askClaude) 구조. 추측으로 설계 시작 금지.
4. 결재판 설계 합의메모 초안(str_replace old/new 명시) → 경년님 합의.
5. 합의 후 str_replace → broker 재기동 → 디스코드 실증(정기 건=관리팀장 승인 카드/스팟=대표 ESCALATE 분기 확인).

## 제약
- 클코 자율실행 금지(명령어 문서로만), 적용 전 코드 읽기 필수, 모름은 추측 금지(확인하려면 [명령어]).
- str_replace·신규생성만(통째 덮어쓰기 금지) / 한 세션=한 파트 / 합의메모 없으면 박제 안 함 / git 자동커밋·푸시 금지(명시 지시 시만).
- 답변 단일 사안 / 결정 합의 후 재검토 금지(새 외부 사실 때만 짚음) / 비개발자 이해 가능하게.
- n8n 절대수정 안 함(참조만) / second-brain vault 읽기만 / 클코 모델 sonnet 기본·opus 어드바이저.

## 환경
- broker=~/.openclaw/workspace/claude-broker (src/agent.js), 재기동=launchctl kickstart -k gui/$(id -u)/com.rentailor.claude-broker
- 봇ID: 관리5738/기획9064/마케팅8104/콘텐츠2535/데이터6609, 채널1510306207573741719. AGENT_BOT_IDS(line 177)=content/marketing/data.
- 관리팀장=opus. 적용 변경=--disallowedTools Task(347줄). 백업 src/agent.js.bak-0333. .env CLAUDE_TIMEOUT_MS=300000(안 건드림).
- 결재 경로 pending/approved/rejected. verdict게이트=agent.js 314~333줄(node fs, Claude도구 아님).
