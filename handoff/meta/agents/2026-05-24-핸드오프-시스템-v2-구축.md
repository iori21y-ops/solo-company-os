---
area: meta/agents
date: 2026-05-24
session: 핸드오프 시스템 v2 (GitHub 공개저장소 + save-handoff 자동화)
---

# 이어가는 중

새 작업 아니라 5월 22~23일 인프라 구축의 연속. 모바일 외부 작업을 위한 솔로 운영 OS 1차 완성.

# 큰 그림

목표: 어디서나(카페/출장/지하철) 새 Claude 세션을 핸드오프 한 번으로 이어받기 + 세션 마무리 한 마디로 박제.

축: ①GitHub 공개저장소 (모바일 web_fetch 가능) ②태그 매핑 자동화 ③iOS 단축어 → 홈화면.

# 이번 세션 완료한 것

1. scripts/save-handoff.sh 생성
   - 태그(랜테일러/에이전트/일회성) → 영역 매핑
   - HANDOFF_CONTENT 환경변수 또는 stdin 파이프로 본문
   - 날짜 파일 → handoff-latest.md → 금지 패턴 점검 → README 갱신 → push → 200 검증 원스텝
   - 신규 영역은 풀 경로(meta/agents 등)로 호출 가능

2. scripts/README.md 갱신 (40줄 추가)
   - save-handoff.sh 사용법 + Claude 자연어 패턴 문서화

3. iOS 단축어 "이어받기" 4칸 메뉴 완성
   - 랜테일러 → projects/rentailor/handoff-latest.md
   - 에이전트 → meta/agents/handoff-latest.md
   - 일회성 → scratch/general/handoff-latest.md
   - 취소
   - 홈화면 아이콘 박힘 (단축어 기본 녹색)

4. Claude 사용자 메모리 #23 갱신
   - 핸드오프 시스템 v1(로컬 ~/projects/_meta + pbcopy) → v2(GitHub 공개저장소 + 태그 매핑)

# 핵심 결정

- 태그 표준: 랜테일러 / 에이전트 / 일회성 (한글 그대로)
- 추론 우선순위: 페치한 area 프론트매터 → 메모리 매핑 → 사용자 재확인
- 본문 전달: HANDOFF_CONTENT 환경변수 우선, 없으면 stdin
- 새 영역 추가 패턴: new-handoff.sh로 디렉토리 생성 → save-handoff.sh 풀 경로 호출 (case문 추가는 자주 쓰일 때만)

# 시행착오 (다음 세션 참고)

- 명령 안에 heredoc 코드블록을 마크다운으로 렌더링하면 깨짐 → 명령은 잘게 쪼개서 던지기
- 클코가 명령 외 추가 작업을 자율로 함 (이번엔 handoff-latest.md도 함께 갱신) → 결과 확인 필수
- 사용자 메모리 한 줄 500자 제한 있음 → 길면 잘라야 함

# 미해결 / 다음 작업

- 쌍3 sales↔cs 경계 정의 (메타-에이전트 영역, 가장 임박)
- 쌍4 ops↔data, 쌍5 strategy, 쌍6 legal↔finance 경계 (순차)
- scripts/refresh-readme.sh 실제 작동 여부 확인 (호출은 되지만 README 갱신 결과 검증 안 함)
- 새 영역 추가 시 save-handoff.sh case 문에 한글 태그 추가하는 흐름 표준화

# 다음 세션 시작 방법

홈화면 → "이어받기" → "에이전트" → Claude 앱 새 채팅에 붙여넣기

또는 직접 URL:
https://raw.githubusercontent.com/iori21y-ops/solo-company-os/main/handoff/meta/agents/handoff-latest.md
