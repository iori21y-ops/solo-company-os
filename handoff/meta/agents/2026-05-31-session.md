# 핸드오프 — 결재판 흐름 봇 코드 연결 (파트B)
area: meta/agents
date: 2026-05-31

## 이어가는 중
9에이전트 OS 디스코드 구현 트랙. 파트A(서버 자동 점검) 완료, 파트B가 다음.

## 완료 (직전 세션 — 파트A)
- server-health-check.sh 생성 (~/projects/_meta/scripts/): 12개 항목 점검
  - 디스크/메모리/claude-broker/OpenClaw/n8n Docker/Ollama/Tailscale/포트(5678,9722)/LaunchAgent/broker로그에러/rentailor.co.kr접근/Docker디스크/대용량로그
  - 각 항목에 복구 명령어 포함 (→ 복구: ... 형식)
- launchd 등록: com.rentailor.health-check.plist (매일 07:00)
- Claude 분석 보고: 스크립트가 원시데이터 수집 → claude -p sonnet으로 분석 → 관리팀장 봇 토큰으로 디스코드 채널에 보고서 게시
- 복구 명령어 저장: health-check-fixes.sh에 자동 복구 가능 명령어 별도 저장
- 복구 승인 흐름: agent.js에 "복구 승인" 핸들러 추가 완료 — fixes.sh 읽어서 execSync 실행 → 결과 보고 → fixes 파일 비움
- 웹훅 fallback: Claude/봇 실패 시 디스코드 웹훅으로 원시 데이터 발송
- 웹훅 URL: https://discordapp.com/api/webhooks/1510306626974777386/GdJ7TVdkbjoaEkSHnGDWPn562D8CiCxR0GA0-joplEWqOeQLibCZWabny0I2Fmy67tkc
- ALLOWED_CHANNEL_IDS=1510306207573741719 (.env에 설정 완료)
- agent.js import 추가: readFileSync/existsSync/writeFileSync from node:fs, execSync from node:child_process

## 이번 세션 작업: 파트B — 결재판 흐름 봇 코드 연결
### 목표
사장님이 디스코드에서 산출물 승인/반려 → 관리팀장이 파일 이동 실행
- 각 에이전트 outputs/pending/ 에 산출물 올라옴
- 사장님이 승인 → pending/ → approved/ 이동
- 사장님이 반려 → pending/ → rejected/ 이동

### 구현 필요 사항
1. agent.js에 파일 이동 명령어 핸들러 추가
   - "승인 [에이전트] [파일명]" 또는 "반려 [에이전트] [파일명]"
   - pending 목록 조회 명령어도 필요 ("대기 목록" 등)
2. 에이전트가 산출물 생성 시 pending/에 저장하는 흐름 (SKILL.md에 이미 규칙 있음)
3. 승인/반려 결과를 디스코드에 보고

### agent.js 현재 구조 (파트A에서 확인)
- 74: extractPrompt()
- 77~81: 모델 변경 커맨드 (prompt.match(/^모델\s+.../))
- 82~?: 복구 승인 커맨드 (prompt.match(/^복구\s*승인$/)) ← 파트A에서 추가
- 봇 간 대화 제한, askClaude, parseCallProtocol, [승인]/[반려] 감지, replyInChunks, [CALL:], [ESCALATE]
- ESM 모듈 (import 방식, require 사용 불가)
- 이미 import된 것: spawn, discord.js, readFileSync/existsSync/writeFileSync, execSync

## 절대 원칙
1. 클코 자체 진행 금지 — 명령어 문서로만
2. 코드 통째 덮어쓰기 금지, str_replace로만
3. 한 세션 = 한 파트
4. 합의메모 없으면 박제 안 함
5. git 자동 커밋 금지

## 환경
- broker=~/.openclaw/workspace/claude-broker, main.js+agent.js+agents.config.js(src/)
- LaunchAgent=com.rentailor.claude-broker
- 토큰 .env (DISCORD_BOT_TOKEN, ALLOWED_CHANNEL_IDS=1510306207573741719)
- 관리팀장=opus, 나머지=sonnet
- 봇ID: 관리~5738/기획~9064/마케팅~8104/콘텐츠~2535/데이터~6609
- 산출물 폴더: ~/projects/_meta/agents/{에이전트}/outputs/{pending,approved,rejected}
- 심볼릭링크: ~/.openclaw/workspace/agent-outputs → ~/projects/_meta/agents/
- 10에이전트: admin,strategy,marketing,content,data,sales,cs,ops,finance,legal

## 첫 지시
1. agent.js 전체 구조 파악 (sed로 범위별 출력)
2. 파일 이동 명령어 설계 합의메모 작성
3. 추측 금지, 코드 수정 금지, 설계만
